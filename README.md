# SmartTrack IoT Supply Chain
Scaffold: backend (Node/Express), frontend (React + MUI), MQTT simulator, Docker + Firebase config.

## Frontend
- Start: cd frontend && npm install && npm start
- Use .env.local to set Firebase config (see .env.example)

## Notes
- Firebase project: smarttrack-iot-supply-chain (set real config for hosting and auth)
